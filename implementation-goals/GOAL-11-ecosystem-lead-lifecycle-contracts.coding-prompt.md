# Goal 11 Coding Prompt - Ecosystem Lead Lifecycle Contracts

Use this prompt for the next Goal 11 chunk.

Implement the next unfinished chunk of Goal 11. Start with `docs/IMPLEMENTATION_STATE.md`, the Goal 11 execution pack, and `implementation-goals/GOAL-11-ecosystem-lead-lifecycle-contracts.lifecycle-contracts.md`. Preserve Leads as the consent-aware non-registered warm-contact ledger. Do not move registered identity, campaign execution, notification delivery, payment ownership, logging storage, CRM workflow, or AI model ownership into Leads.

Chunk 11.2 is complete as documentation-only contract definition. For chunk 11.3, define Auth-backed tenant and admin access requirements before replacing the temporary browser-entered internal-token admin shell.

Use minimized events and guarded APIs by default. Do not include raw contact values, raw messages, confirmation tokens, private URLs, or metadata values unless the owner explicitly approves the exact fields, destination, retention, and validation plan in the active task. Do not implement runtime source changes until the pre-coding gate passes and exact file scope is named.
