# Goal 9 Coding Prompt

Add focused contract tests for a minimized AI/CRM lead context builder. The builder must be pure and local, emit only derived or non-identifying fields, and never include contact values, raw message text, confirmation tokens, source URL paths/query strings, private URLs, or raw metadata values.
