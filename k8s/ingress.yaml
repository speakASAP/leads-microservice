apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: leads-microservice
  namespace: statex-apps
  labels:
    app: leads-microservice
    managed-by: k8s-migration
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - leads.alfares.cz
      secretName: leads-microservice-tls
  rules:
    - host: leads.alfares.cz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: leads-microservice
                port:
                  number: 4400
