apiVersion: apps/v1
kind: Deployment
metadata:
  name: leads-microservice
  namespace: statex-apps
  labels:
    app: leads-microservice
    managed-by: k8s-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: leads-microservice
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: leads-microservice
    spec:
      containers:
        - name: app
          image: localhost:5000/leads-microservice:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4400
              name: http
          envFrom:
            - configMapRef:
                name: leads-microservice-config
            - secretRef:
                name: leads-microservice-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 4400
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 4400
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
